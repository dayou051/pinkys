---
import Banner from '../../assets/banner.jpg'
import Button, { TypeButton } from '../common/Button'
---

<section class="flex flex-col md:flex-row 2xl:w-2xl 2xl:mx-auto">
    <img class="md:w-1/2" src={Banner.src} />
    <div class="flex flex-col p-5 gap-8 md:px-14 md:justify-center md:gap-16">
        <div class="flex flex-col text-pink-600 gap-2">
            <span class="tracking-widest font-extralight italic">PERSONALIZADO</span>
            <h1 class="text-4xl font-extrabold">Diseña tus detalles</h1>
            <p class="font-[400]">Crea detalles únicos para tus seres queridos con nuestro configurador.</p>
        </div>
        <Button type={TypeButton.SECUNDARY} selfStart client:load>Personalizar Ahora</Button>
    </div>
</section>